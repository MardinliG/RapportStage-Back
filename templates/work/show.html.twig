{% extends 'base.html.twig' %}

{% block title %}Work{% endblock %}

{% block body %}
    <h1>Work</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ work.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_index') }}">back to list</a>

    <a href="{{ path('app_work_edit', {'id': work.id}) }}">edit</a>

    {{ include('work/_delete_form.html.twig') }}
{% endblock %}
